<% if book.favorited_by?(current_user) %>
     <%=link_to blog.likes.count, blog_likes_path(blog.id), method: :delete, remote: true, class="fa-solid fa-heart" %>
<% else %>
     <%=link_to blog.likes.count, blog_likes_path(blog.id), method: :post, remote: true, class="fa-light fa-heart" %>
<% end %>